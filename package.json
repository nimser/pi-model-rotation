{
  "name": "@nimser/pi-model-rotation",
  "version": "0.9.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "description": "Quota-aware model rotation across every AI subscription you pay for",
  "keywords": [
    "pi-package"
  ],
  "bin": {
    "pi-rotation-quota": "./bin/quota.ts"
  },
  "scripts": {
    "test": "bash tests/run-all.sh"
  },
  "pi": {
    "extensions": [
      "./extension/index.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "peerDependenciesMeta": {
    "@earendil-works/pi-coding-agent": {
      "optional": true
    }
  },
  "engines": {
    "node": ">=26"
  }
}
